import { Input } from "@/app/ui/components";
import { LookupIcon } from "@/app/ui/components/CatalogueSection";

export const Search = () => {
  return (<Input onChange={()=>{}} variant={"white"} icon={<LookupIcon/>}/>)
}